pipeline {
    agent any

    stages {

        // stage('Docker') {
        //     steps {
        //         sh 'docker build -t -f ci/Dockerfile my-playwright .'
        //     }

        stage('Build Docker images ') {
            steps {
                sh 'docker build -t -f ci/Dockerfile-playwright my-playwright .'
                sh 'docker build -t -f ci/Dockerfile-aws-cli my-aws-cli .'
            }
        }
    }
}