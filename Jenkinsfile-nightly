pipeline {
    agent any

    stages {

        // stage('Docker') {
        //     steps {
        //         sh 'docker build -f ci/Dockerfile my-playwright .'
        //     }

        stage('Build Docker images ') {
            steps {
                sh 'docker build -f ci/Dockerfile-playwright my-playwright .'
                sh 'docker build -f ci/Dockerfile-aws-cli my-aws-cli .'
            }
        }
    }
}